DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/birsistem?schema=public"
NEXT_PUBLIC_SITE_URL="https://birsistem.az"

# Admin panel (/admin). Both are required; without them every login is refused.
# ADMIN_SESSION_SECRET: at least 32 random characters, e.g. `openssl rand -base64 32`.
ADMIN_PASSWORD=""
ADMIN_SESSION_SECRET=""
